# CEX Backend

Install dependencies:

```bash
npm install
```

Run the TypeScript server in development:

```bash
npm run dev
```

Build and run the compiled JavaScript:

```bash
npm run build
npm start
```
